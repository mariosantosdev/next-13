import Link from './Link'

/*
    To create new pages to the application now all files inside the folder app that contains "page" 
    will be transformed in pages
*/
export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <Link />
    </div>
  )
}
